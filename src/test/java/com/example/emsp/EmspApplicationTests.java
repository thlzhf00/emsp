package com.example.emsp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EmspApplicationTests {

	@Test
	void contextLoads() {
		// This test simply checks if the Spring application context loads without errors.
		// If it loads, it means all components (controllers, services, repositories)
		// are correctly defined and their dependencies can be resolved.
	}

}

